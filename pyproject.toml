[project]
name = "spacetime-attention"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.7.1",
]

[tool.flake8]
max-line-length = 120
ignore = ["E501", "E203"]